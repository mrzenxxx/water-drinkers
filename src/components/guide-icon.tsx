import { createLucideIcon } from 'lucide-react';

/**
 * Книга с вопросительным знаком — значок руководства (`/guide`).
 *
 * Готового такого в lucide нет: есть книга и есть вопрос в круге, но круг
 * читается как «справка по этому месту», а руководство — это книга, которую
 * открывают целиком. Поэтому значок собран из двух штатных: контур `Book` и
 * вопрос из `CircleHelp`, уменьшенный и поставленный на обложку. Сделан через
 * `createLucideIcon`, так что это обычный значок lucide: тот же штрих, те же
 * пропсы, годится в `IconChip` и `PageHeader`.
 */
export const BookHelp = createLucideIcon('book-help', [
  [
    'path',
    {
      d: 'M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H19a1 1 0 0 1 1 1v18a1 1 0 0 1-1 1H6.5a1 1 0 0 1 0-5H20',
      key: 'book',
    },
  ],
  ['path', { d: 'M10.7 7a2.4 2.4 0 0 1 4.66.8c0 1.6-2.4 2.4-2.4 2.4', key: 'question' }],
  ['path', { d: 'M13 13.4h.01', key: 'dot' }],
]);
